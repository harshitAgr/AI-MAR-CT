# AI-MAR-CT


 [![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)](https://opensource.org/licenses/MIT)
[![Made With Love](https://img.shields.io/badge/Made%20With-Love-red.svg)](https://github.com/chetanraj/awesome-github-badges)
[![DOI](https://zenodo.org/badge/744644178.svg)](https://doi.org/10.5281/zenodo.19651643)

 Collection of deep learning-based metal artifact reduction (MAR) articles for CT/CBCT Imaging.

## Citation

If you use this collection, please cite it via the Zenodo DOI. The **concept DOI** below always resolves to the latest release; use a specific version DOI from Zenodo for a pinned citation.

```bibtex
@misc{agrawal_ai_mar_ct,
  author       = {Agrawal, Harshit},
  title        = {{AI-MAR-CT: A curated collection of deep learning-based metal artifact reduction papers for CT/CBCT imaging}},
  howpublished = {\url{https://github.com/harshitAgr/AI-MAR-CT}},
  publisher    = {Zenodo},
  doi          = {10.5281/zenodo.19651643},
  url          = {https://doi.org/10.5281/zenodo.19651643}
}
```

 1. **Deep Learning-Based Metal Artifact Reduction in CT for Total Knee Arthroplasty (KMAR-Net)** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*J. Lee, H.-D. Chae, H. Cho, J. M. Kim, S. H. Hong, J.-Y. Choi, H. J. Yoo, S.-J. Ye* \
**Summary** \
KMAR-Net is a supervised, image-domain CNN designed specifically for post-operative knee CT after total knee arthroplasty (TKA), where large high-atomic-number femoral and tibial implants produce severe streak artifacts that are difficult for generic MAR methods to handle. Unlike dual-domain networks, KMAR-Net does not require projection data at inference, does not use an explicit metal segmentation step, and does not rely on a-priori knowledge of implant shape — the metal-corrupted axial slice is fed directly to the network and the artifact-reduced slice is produced end-to-end, which sidesteps the secondary-artifact problem caused by segmentation errors in projection-completion pipelines. Training data is generated via a sinogram-handling simulation pipeline: axial knee CT scans from 50 patients without any metallic implant are used as artifact-free references, simplified virtual metal shapes mimicking commonly used TKA components (distal femur, proximal tibia, long-stem variants) are manually designed with randomised size, orientation and position, and each metal-inserted image is forward-projected, metal-trace-replaced, and back-projected to obtain a paired artifact/clean slice — 500 pairs per patient, 25,000 pairs in total, split 15,000 / 5,000 / 5,000 for training / validation / simulated-test. A 44-patient clinical test set with prior TKA (18 unilateral, 26 bilateral) is used for the real-world evaluation, and a separate CTDI-phantom experiment (stainless-steel and aluminium rods, varying count, tube voltage 100/120 kVp, slice thickness 1/2 mm, bone and standard kernels) with 474 simulated pairs (330 / 72 / 72) plus 80 real phantom images is used to check feasibility under controlled conditions. The network is implemented in PyTorch 1.1.0; architectural specifics and training hyper-parameters (loss, optimiser, learning rate, inference time, exact GPU model) are deferred to the paper's supplementary material, with source code released at github.com/ljm861/KMAR-Net. On the simulated test set KMAR-Net reaches Pearson correlation coefficient (PCC) 0.995, PSNR 41.49 dB and SSIM 0.996 against the artifact-free reference. On the 44-patient clinical test set the area of dark streak artifacts (≤ −200 HU pixels within the knee outline) drops from 2,165.2±576.5 mm² (Non-MAR) and 1,554.6±566.3 mm² (O-MAR, the commercial projection-based orthopaedic MAR baseline) to 100.6±94.5 mm² (KMAR-Net); mean attenuation within the streaks rises from −724.9±71.8 HU (Non-MAR) / −424.2±59.4 HU (O-MAR) to −247.7±28.0 HU (KMAR-Net), and the intra-streak SD collapses from 295.7±11.7 / 218.8±35.4 HU to 68.5±50.3 HU — all differences significant at p < 0.001. On the phantom, KMAR-Net (PCC 0.994 / PSNR 39.64 dB / SSIM 0.988; mean HU 117.3 / SD 19.2) is the closest to the metal-free reference (mean HU 118.7 / SD 19.9) and clearly beats O-MAR (0.985 / 35.85 / 0.938 ; 108.2 HU / 49.1 SD) and Non-MAR (0.928 / 31.27 / 0.895 ; 94.2 HU / 182.9 SD). Two musculoskeletal radiologists graded KMAR-Net superior to O-MAR for overall artifact level and soft-tissue depiction on all 44 clinical cases (5-point Likert, all P < 0.001), with bone-conspicuity superiority significant for the less-experienced reader (P < 0.001) but not the 6-year reader (P = 0.080); pseudocement artifacts dropped from 37/44 (84%) in O-MAR to 16/44 (36%) in KMAR-Net (P < 0.001). The authors note several limitations: (1) a newly introduced band-like soft-tissue blurring appears in 27/44 (61%) of KMAR-Net cases (absent from O-MAR), concentrated where the original streak burden was most severe, and cannot be explained analytically — a fundamental drawback of data-driven MAR; (2) the sinogram-handling simulation does not fully reproduce real-world physics (cone-beam effects, tube-current modulation, scatter, non-linear partial volume), so there is a residual simulated-to-real domain gap; (3) the 44-case clinical cohort is small, female-dominant, and restricted to four implant families (Vanguard, Attune, LPS-Flex, LCCK), limiting generalizability across sex, anatomy and implant design; (4) the only comparison is against commercial O-MAR — no head-to-head with dual-energy, hybrid, transformer, or diffusion-based DL MAR methods. \
Scientific Reports, 2025. [[doi](https://doi.org/10.1038/s41598-025-21012-7)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://www.nature.com/articles/s41598-025-21012-7) [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/ljm861/KMAR-Net)

 1. **FIND-Net: Fourier-Integrated Network with Dictionary Kernels for Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*F. Tasharofi, F. Fan, M. Qahqaie, M. Thies, A. Maier* \
**Summary** \
FIND-Net is a supervised, image-domain deep-unfolding MAR network that extends DICDNet's convolutional-dictionary model with joint spatial / frequency-domain processing to address CNNs' limited receptive fields. The underlying physical model decomposes a metal-corrupted CT image Y into an artifact-free component X and an artifact term A = Σₙ Kₙ⊗Mₙ, where Kₙ are learnable dictionary kernels that encode metal-artifact patterns and Mₙ are their feature maps; the masked reconstruction objective min_{M,X} ‖I⊙(Y−X−A)‖²_F + λ₁P₁(M) + λ₂P₂(X) is unrolled into 10 iterative stages, each containing an M-Net (updates M via a proximal step) and an X-Net (updates X via a proximal step), both implemented as ResNets whose standard 3×3 convolutions are replaced by FE-ResBlocks (Frequency-Enhanced ResBlocks) built on GFFC — a modified Fast Fourier Convolution that splits channels into a Local Branch for spatial convolutions and a Global Branch that applies FFT → trainable Gaussian filter Gσ,c(u,v) = exp(−((D²−c²)/(D·σ+ε))²) → 1×1 conv → IFFT, with learnable σ and c giving per-layer adaptive frequency selection. The Global-Branch channel fraction α increases across stages from 0.0 to 0.8, so early stages emphasise local spatial refinement while later stages emphasise global frequency-domain artifact suppression; X⁽⁰⁾ is initialised with linear interpolation (LI). Training uses a multi-term stage-wise loss L = Σₛ ωₛ‖I⊙(X−X⁽ˢ⁾)‖²_F + γ₁Σₛ ωₛ‖I⊙(X−X⁽ˢ⁾)‖₁ + γ₂Σₛ ωₛ‖I⊙(Y−X−A⁽ˢ⁾)‖₁, combining a Frobenius reconstruction term with L1 sparsity and artifact-consistency terms. The network is trained on the AAPM CT-MAR Grand Challenge dataset (XCIST-simulated from NIH DeepLesion body scans and UCLH Stroke EIT head scans, with realistic quantum noise / beam hardening / scatter) at 512×512: 5,500 training cases (1,300 head + 4,200 body), 700 validation and 700 test cases, plus a 29-case real-world evaluation set without ground truth. Implementation is in PyTorch on an NVIDIA A100 (80 GB HBM2) with AdamW (β₁=0.9, β₂=0.999), LR 1×10⁻⁴, weight decay 1×10⁻⁵, linear warm-up + cosine annealing, batch size 6, up to 200 epochs with early stopping. Across metal sizes FIND-Net reports average MAE/SSIM/PSNR of 17.9 / 0.952 / 42.8 dB, versus LI (26.6 / 0.913 / 38.9), DICDNet (18.3 / 0.951 / 42.4), OSCNet (18.8 / 0.948 / 41.7), OSCNet+ (18.8 / 0.948 / 42.0), and the FIND-Net (No-GF) ablation (18.2 / 0.951 / 42.5); the relative improvements over the DICDNet baseline — −3.07% MAE, +0.18% SSIM, +0.90% PSNR — are statistically significant at the 95% CI level. On seven artifact-free anatomical patches (where an ideal MAR should not change the image), FIND-Net achieves the best preservation (MAE 0.005 / SSIM 0.97 / PSNR 43.23 dB) vs. DICDNet-level baselines (~MAE 0.007 / SSIM 0.94 / PSNR 40.4 dB). Computational complexity drops ~16% (334.45 → 279.55 GFLOPs) via FFT-based operations, but per-image inference time grows from 0.15 s (DICDNet) to 1.07 s because of the frequency-domain transforms — the authors flag this latency overhead as the method's main practical limitation; they additionally observe that OSCNet / OSCNet+ underperform DICDNet on this benchmark, suggesting their rotational-symmetry encoding generalises less well than FIND-Net's frequency-adaptive formulation. \
[[arXiv](https://arxiv.org/abs/2508.10617)] MICCAI, 2025. [[doi](https://doi.org/10.1007/978-3-032-05169-1_19)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://papers.miccai.org/miccai-2025/paper/1842_paper.pdf) [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/Farid-Tasharofi/FIND-Net)

 1. **Balancing Efficiency and Restoration: Lightweight Mamba-Based Model for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*W. Qu, S. Liang, X. Li, C. Pan, A. Yan, A. Elazab, S. Niu, D. Zeng, X. Wan, C. Wang* \
**Summary** \
MARMamba is a lightweight, image-domain-only supervised MAR network that replaces standard Transformer attention with state-space (Mamba) blocks inside a streamlined U-Net, aiming for a favourable trade-off between artifact suppression, parameter count, and memory. The backbone takes a B×1×H×W metal-corrupted CT image, lifts it to D=12 channels via a 3×3 convolution, and routes features through a symmetric encoder-decoder with Multi-Scale Mamba (MS-Mamba) blocks configured sequentially as [1, 2, 2, 4, 2, 2, 1, 1] across the down/up stages. Each MS-Mamba is built from two key components: (i) a Flip Mamba Block (FMB) that runs three parallel Mamba instances on the original, horizontally-flipped, and vertically-flipped feature maps to pool streak information from multiple directions that single-pass SSM scans would otherwise miss; and (ii) an Average Maximum Feed-Forward Network (AMFN) that fuses max-pooled (dominant artifact response) and average-pooled (global structure) pathways before the residual merge. Because MARMamba operates only on images, it needs no sinogram access at inference—simplifying deployment on post-processing pipelines. It is trained supervised on SynDeepLesion with 1,000 paired metal-corrupted/metal-free training images and 200 test pairs, using a progressive multi-resolution schedule: 256×256 (batch 8, 100k iterations) → 336×336 (batch 4, 200k iterations) → 416×416 (batch 2, 20k iterations), optimised with Adam + CosineAnnealingLR. The loss is α·Pseudo-Huber (α=0.8) + β·LPIPS perceptual (β=0.2) with a weighting coefficient c=0.03; total training takes 27.31 hours on a single NVIDIA RTX 3090. On the SynDeepLesion test set MARMamba reports, by artifact size, PSNR / SSIM / RMSE of 47.60±1.65 dB / 0.9943±0.0014 / 1.26±0.23 (large), 47.90±3.20 dB / 0.9948±0.0014 / 1.28±0.45 (medium), 50.91±1.33 dB (small) and 52.03±0.95 dB (tiny), outperforming InDuDoNet, OSCNet, MEPNet, DuDoDp and MARformer; the Transformer-based MARPVT is competitive (46.62 dB PSNR on large) with slightly faster inference. MARMamba has 0.59M parameters—the smallest among the compared baselines—and runs at 0.0393±0.0008 s per 416×416 image on the RTX 3090. The authors note two main limitations: (1) restoration is suboptimal in low-contrast regions where feature extraction is weaker, and (2) the purely synthetic SynDeepLesion training set fails to capture the full diversity of real implants, producing a domain gap on real-world CLINIC-metal data, where MARMamba (5-point radiologist score 3.748±1.271) slightly trails OSCNet+ (3.863±1.120). \
[[arXiv](https://arxiv.org/abs/2604.06622)] IEEE Transactions on Radiation and Plasma Medical Sciences, 2026. [[doi](https://doi.org/10.1109/TRPMS.2026.3674764)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/RICKand-MORTY/MARMamba)

 1. **NAFMAR: X-Ray CT Metal Artifact Reduction Using Neural Attenuation Field Prior** <img src="https://img.shields.io/badge/Self-Supervised-blue.svg" alt="Self-Supervised"> \
*J. Lee, S. Kim, J. Ahn, A. S. Wang, J. Baek* \
**Summary** \
NAFMAR is a self-supervised, per-case sinogram-domain inpainting method for CBCT metal artifact reduction that uses a Neural Attenuation Field (NAF) as a learned prior, removing the need for paired training data or any pretraining step. The NAF is an implicit neural representation that maps 3D spatial coordinates (after a multi-resolution hash encoding with a 2¹⁶-entry hash table and 4-dim features per level) through a compact MLP of 4 fully-connected ReLU layers with 64 channels each; the whole field has roughly 4.5 M trainable parameters (~17 MB). The pipeline has three stages per scan: (1) fit the NAF to the measured projections using Metal Trace-Masked Ray Sampling, which partitions rays into a non-metal set R_r and a metal-affected set R_m so that the metal-corrupted line integrals do not dominate fitting; (2) render a prior image from the optimized NAF and use it to inpaint the metal traces in the original sinogram; and (3) reconstruct the artifact-reduced volume with FBP. Optimization uses a two-term self-supervised loss L = L_RECON(θ, R_r) + λ_M · L_MA(θ, R_m) with λ_M = 1, where L_RECON is a squared-L2 fidelity on non-metal rays and L_MA is a weight-modulated squared-L2 on the metal-affected rays (w(r) attenuates their contribution). Optimization runs for 150 epochs (~77 K steps) per case on a single NVIDIA RTX 3090, taking roughly 15 minutes with ~4 GB of GPU memory. Experiments use dental CT volumes from TCIA with 2–5 manually inserted metal implants for the simulated study and the CLINIC-metal dataset (512×512×187 voxels) for the clinical evaluation; reconstructed CBCT volumes are 512×512×100 at 0.4×0.4×0.4 mm voxels. A CNN baseline (DSCMAR) is trained on 1,551 metal-free / metal-corrupted image pairs for comparison. With 4 metal implants, NAFMAR attains PSNR 34.42 dB / SSIM 0.931, versus DSCMAR (34.43 dB / 0.914) and CNNMAR (33.73 dB / 0.909), i.e. matching the supervised DSCMAR on PSNR while achieving the best SSIM among the compared methods. Total per-case inference is 7.2 s (0.6 s prior generation + 3.1 s sinogram inpainting + 3.5 s FBP). The authors note a primary limitation: the size of metal objects directly affects reconstruction quality—large metal implants produce large metal traces that compromise data fidelity during NAF fitting, so residual artifacts can persist in extreme cases involving large metallic objects. \
Medical Physics, 2025. [[doi](https://doi.org/10.1002/mp.17859)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://pmc.ncbi.nlm.nih.gov/articles/PMC12257910/)

 1. **A Strategy for Simulation-Driven CT Metal Artifact Reduction Toward Improving Network Generalizability** <img src="https://img.shields.io/badge/Self-Supervised-blue.svg" alt="Self-Supervised"> \
*S. Yun, S. Hyun, D.-I. Choi, S. Cho* \
**Summary** \
This paper proposes a self-supervised framework for CT MAR that integrates physics-informed correction with generative deep learning, eliminating the need for real paired datasets. The method consists of two main components: (1) a lightweight multi-layer perceptron (MLP) that performs physics-driven polynomial beam-hardening correction with implicit sinogram consistency built into its optimization objective, enabling case-specific adaptation; and (2) a conditional latent diffusion model (LDM) trained to remove residual artifacts after MLP correction. A key innovation is the simulation strategy: the learned MLP parameters are reused to generate artifact-contaminated images from artifact-free scans, producing pseudo paired data that enables self-supervised training of the LDM without requiring real paired datasets. By operating in a low-dimensional latent space, the LDM significantly reduces inference time compared to pixel-space diffusion models. The method was evaluated on both the SynDeepLesion dataset and real clinical data, demonstrating superior artifact removal and structural preservation compared to state-of-the-art MAR techniques. The authors also won the AAPM CT-MAR Grand Challenge using a related approach. The framework specifically addresses limitations of existing DL-based MAR methods, which lack explicit physical modeling of artifact formation and rely heavily on data-driven mappings, making them prone to hallucination and anatomical distortion. \
Medical Physics, 2026. [[doi](https://doi.org/10.1002/mp.70336)]

 1. **PDuMSRNet: Prompt Guiding Multi-Scale Adaptive Sparse Representation-Driven Network for Low-Dose CT MAR** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*B. Shi, B. Chen, S. Zhang, H. Fu, Z. Hu* \
**Summary** \
PDuMSRNet is a supervised, model-interpretable dual-domain framework that jointly addresses low-dose CT reconstruction and metal artifact reduction (LDMAR) using a single model across multiple dose levels. The image-domain backbone, PMSRNet, is inspired by multi-scale sparsifying frames and features a four-scale architecture (filter sizes 5×5 to 11×11, output channels 25–121). Two key modules drive the design: (1) a Prompt Guiding Scale-Adaptive Threshold Generator (PSATG) that fuses local (convolutional residual), regional (window attention), and global (anchored stripe attention) features together with a prompt comprising the dose level, metal mask, and input instance to produce scale-adaptive thresholds; and (2) a Multi-Scale Coefficient Fusion Module (MSFuM) based on double cross-attention that merges high- and low-resolution features across scales. PDuMSRNet unfolds T stages, each containing a sinogram-domain s̃-Net (three residual blocks) and an image-domain x-Net (PMSRNet). Training uses a hybrid loss combining weighted MSE (ω₁ = 0.01) in both image and sinogram domains with a multi-scale perceptual loss (ω₂ = 1×10⁻⁴) based on pre-trained ResNet-50. The model is trained on the DeepLesion dataset with 1,000 FDCT images × 80 metal masks (80,000 synthetic pairs, 416×416 images, 641×640 sinograms) for 100 epochs with batch size 1, and tested at 1/2, 1/4, and 1/8 dose levels (1×10⁵, 5×10⁴, 2.5×10⁴ incident photons at 120 kVp). PDuMSRNet outperforms 13 single-dose methods (FBP, REDCNN, InDuDoNet, DuMSRNet, CTformer, LIT-Former, etc.) and 3 multi-dose methods (AirNet, PromptIR, BDuMSRNet), achieving average PSNR/SSIM of 42.26/0.9771 (1/2 dose), 41.58/0.9742 (1/4 dose), and 40.44/0.9667 (1/8 dose). Storage is approximately one-third that of training separate DuMSRNet models per dose level. The authors note that the network occasionally fails to distinguish massive streaking artifacts from true anatomical tissue for large metal implants, and current methods exhibit blurring on clinical data due to domain gap between simulated training data and clinical test data. \
[[arXiv](https://arxiv.org/abs/2504.19687)] Medical Image Analysis, 2026. [[doi](https://doi.org/10.1016/j.media.2025.103870)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/shibaoshun/PDuMSRNet.git)

 1. **LDMNMAR: Metal Artifact Reduction Algorithm With Conditional Latent Diffusion Model for Dental Cone-Beam CT** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*D.-I. Choi, S. Yun, S. Hyun, S. Cho* \
**Summary** \
LDMNMAR is a supervised metal artifact reduction method for dental CBCT that combines a conditional latent diffusion model (LDM) with an improved normalized metal artifact reduction (NMAR) pipeline. The method operates in multiple stages: first, a diffusion network trained in the latent space of a CVQ-VAE autoencoder estimates a clean image latent variable from noise, conditioned on the metal-artifact-corrupted image latent variables, to synthesize an artifact-corrected CBCT image. This LDM output then serves as a high-quality anatomical prior for a modified NMAR framework. The NMAR pipeline is augmented with a dedicated metal segmentation network and a secondary artifact correction network to suppress residual artifacts. The network is trained in a supervised manner on paired sets of metal-artifact-corrupted and clean reconstructed images using a combination of mean squared error (MSE) and learned perceptual image patch similarity (LPIPS) losses. Clinical dental CBCT projection data from 21 patients were used for evaluation on 512 × 512 images. Compared to CNNMAR, LDMNMAR improves RMSE from 34.78 × 10⁻⁴ to 19.30 × 10⁻⁴, PSNR from 49.3 to 54.3 dB, and SSIM from 91.2% to 97.2%. The total inference time for 350 images is approximately 202 seconds, including 109 s for LDM inference (0.314 s per image), 14 s for image tuning, 3 s for metal segmentation, and 74 s for reconstruction and NMAR. The authors note that diffusion model outputs inherently suffer from structural blurring (since inference is derived from noise) and are prone to hallucination errors—the modified NMAR step is specifically designed to mitigate these concerns. Additional challenges include the domain gap in CBCT data due to higher noise and scatter levels, as well as CBCT-specific artifacts such as cone-angle artifacts and limited field of view. \
Journal of Applied Clinical Medical Physics, 2025. [[doi](https://doi.org/10.1002/acm2.70317)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://aapm.onlinelibrary.wiley.com/doi/10.1002/acm2.70317)

 1. **TDMAR-Net: A Frequency-Aware Tri-Domain Diffusion Network for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*W. Chen, B. Ning, Z. Zhou, L. Shi, Q. Liu* \
**Summary** \
TDMAR-Net is an unsupervised diffusion model-based network that uniquely operates across three domains—projection (sinogram), image, and Fourier—to reduce metal artifacts in CT images. A high-pass filter module in the Fourier domain adjusts the weights of high-frequency and low-frequency components, while block-wise processing extracts diffusion priors that are iteratively introduced into the sinogram and image domains to fill metal-induced artifact regions. The framework employs a two-stage training strategy combining large-scale pretraining with masked data fine-tuning to enhance accuracy and adaptability. Validated on both synthetic and clinical datasets, TDMAR-Net demonstrates superior performance compared to existing unsupervised MAR methods. \
Physics in Medicine & Biology, 2025. [[doi](https://doi.org/10.1088/1361-6560/ae0efc)]

 1. **Bi-Constraints Diffusion: A Conditional Diffusion Model With Degradation Guidance for Metal Artifact Reduction** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*M. Luo, N. Zhou, T. Wang, L. He, W. Wang, H. Chen, P. Liao, Y. Zhang* \
**Summary** \
BCDMAR is an unsupervised metal artifact reduction method that combines iterative reconstruction with a conditional score-based diffusion model. Unlike traditional approaches that use metal-excluded projection operators, BCDMAR introduces a metal artifact degradation operator in the data-fidelity term and employs a pre-corrected image as a prior constraint to guide the diffusion model's generation process, effectively preventing grayscale shifts and unreliable structures. By iteratively applying the score-based diffusion model and the data-fidelity step in each sampling iteration, the method maintains reliable tissue representation around metal regions while producing highly consistent structures in non-metal regions. Extensive experiments demonstrate BCDMAR's superior performance over state-of-the-art unsupervised methods (ADN, ACDNet) and supervised methods (DuDoNet, InDuDoNet+) both quantitatively and visually. \
IEEE Transactions on Medical Imaging, 2025. [[doi](https://doi.org/10.1109/TMI.2024.3442950)]

1. **Dual-Encoder Architecture for Metal Artifact Reduction in kV-Cone-Beam CT for Head and Neck Cancer Radiotherapy** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*J. Ki, J. M. Lee, W. Lee, J. H. Kim, H. Jin, S. Jung, J. Lee* \
**Summary** \
This work targets metal artifacts in linear-accelerator-mounted kV-CBCT used for head-and-neck adaptive radiotherapy, where artifacts corrupt both the daily CBCT and, via deformable image registration (DIR), the synthetic planning CT that drives dose calculation. The authors argue that a single CNN must simultaneously (a) remove the streaks and (b) restore the corrupted anatomy, and that these two objectives pull in opposite directions for a single-encoder network. They therefore propose an image-domain dual-encoder U-shape network: Encoder1 is a UNet-style branch (convolutional blocks + max-pooling) tuned for aggressive artifact removal — max-pooling is well-suited here because metal-artifact pixels have anomalously high values — while Encoder2 is a modified FusionNet branch (residual convolutional blocks, strided convolutions instead of max-pooling) tuned for anatomy preservation. The decoder receives a convolutional feature from Encoder1 and an attentional feature from Encoder2, fused through a channel-wise attention block that sits on top of a spatial-attention-processed feature map; this "spatial + channel-wise attention" fusion is the proposed final model. For ablation the authors also build four single-encoder baselines (UNet, FusionNet, UNet/L, FusionNet/L where /L denotes a larger encoder that merges residual blocks / extra max-pooling / spatial attention) and two weaker dual-encoder variants (simple-addition fusion; spatial-attention-only fusion). Training is supervised with paired synthetic CBCTs generated from a CBCT dataset acquired on a Varian VitalBeam LINAC's C-arm CBCT at Seoul National University Hospital (2019–2020): 7 patients / 651 slices provide metal-artifact-free references, 24 patients / 2,232 slices provide metal-corrupted slices used only to extract realistic metal shapes (HU ≈ 8,000, erosion + Gaussian blur on the mask, small position perturbation), and the position-corrected metal is forward-projected via Radon transform, scaled to produce variable artifact strengths, and inverse-Radon-transformed into paired inputs/targets. The synthesized corpus totals 29,104 training / 3,795 validation / 7,201 simulated-test pairs, and a separate 34-patient / 3,155-slice real-world inference set is used for the clinical artifact-index evaluation. The supervised objective sums (i) pixel-wise MSE on the output, (ii) SSIM loss on the output, and (iii) an auxiliary MSE loss applied to intermediate Encoder2 features (convolved to image resolution) so that the over-preserving FusionNet branch is pushed to cleanly separate metal-artifact residual from anatomy. Training runs for 100 epochs with batch size 32 and random-crop augmentation; per-image inference time and specific GPU hardware are not reported in the manuscript. On the simulated test set, the proposed dual-encoder + spatial/channel attention achieves PSNR 41.86 dB, MSE 8.6×10⁻⁵, SSIM 0.9931, PCC 0.9937 — the best across all seven architectures: single UNet (41.00 / 1.07×10⁻⁴ / 0.9918 / 0.9927), single FusionNet (39.92 / 1.42×10⁻⁴ / 0.9903 / 0.9912), UNet/L (39.30 / 1.47×10⁻⁴ / 0.9903 / 0.9908), FusionNet/L (39.99 / 1.30×10⁻⁴ / 0.9910 / 0.9917), dual-encoder + simple-add (40.43 / 1.18×10⁻⁴ / 0.9915 / 0.9920), dual-encoder + spatial-attention only (40.73 / 1.13×10⁻⁴ / 0.9921 / 0.9924). On the 34-patient clinical set the proposed model also achieves the lowest mean and narrowest distribution of the artifact index (√(SD_artifact² − SD_clean²)) across all seven architectures, with qualitative analysis confirming simultaneous streak removal and tooth/soft-tissue restoration that the single-encoder UNet (tends to produce grayish/blurry tissue) and single-encoder FusionNet (over-preserves residual artifacts) cannot achieve alone. The authors flag three limitations: (1) all CBCT data come from a single institution (SNUH) on one LINAC model, so cross-scanner / cross-center generalisation is untested; (2) only CNN architectures are explored — Transformer or diffusion-based alternatives are left to future work; (3) when the MAR-cleaned CBCT is used to generate a deformed synthetic pCT, structural volumetric similarity is lower than expected because residual artifacts on the commercial-O-MAR pCT (rather than on the CBCT) still hinder DIR, pointing to the need to apply DL-MAR to both pCT and CBCT in the adaptive-RT pipeline. \
Scientific Reports, 2024. [[doi](https://doi.org/10.1038/s41598-024-79305-2)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://www.nature.com/articles/s41598-024-79305-2)

1. **Unsupervised CT Metal Artifact Reduction by Plugging Diffusion Priors in Dual Domains** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*X. Liu, Y. Xie, S. Diao, S. Tan, X. Liang* \
**Summary** \
DuDoDp-MAR is an unsupervised dual-domain metal artifact reduction method that leverages diffusion model priors without requiring paired training data. The approach first trains a diffusion model on artifact-free CT images, then iteratively applies the learned priors in both sinogram and image domains to restore metal-corrupted regions. A key innovation is the use of temporally dynamic weight masks for image-domain fusion. Trained and evaluated on DeepLesion-based synthetic data (90 metal masks for training, 10 for testing) and clinical datasets, DuDoDp-MAR achieves the best overall performance among unsupervised methods, surpassing Score-MAR (another diffusion-based approach), ADN, and even the supervised CNNMAR, while demonstrating superior visual quality on clinical data. \
[[arXiv](https://arxiv.org/abs/2308.16742)] IEEE Transactions on Medical Imaging, 2024. [[doi](https://doi.org/10.1109/TMI.2024.3351201)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/DeepXuan/DuDoDp-MAR)

1. **DCDiff: Dual-Domain Conditional Diffusion for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*R. Shen, X. Li, Y.-F. Li, C. Sui, Y. Peng, Q. Ke* \
**Summary** \
DCDiff is a supervised dual-domain diffusion framework for CT MAR that conditions the image generation process on both image-domain and sinogram-domain information. In the image domain, the raw metal-corrupted CT image and the filtered back-projection (FBP) of the metal trace are used as conditions; in the sinogram domain, a novel Diffusion Interpolation (DI) algorithm generates sinogram priors by training a dedicated diffusion model to inpaint the metal-corrupted regions. Two UNet-based denoising networks (~26M parameters total) are trained on paired metal-corrupted/metal-free CT images from the DeepLesion dataset. Ablation studies show that adding DI improves PSNR/SSIM from 31.59 dB/0.9124 to 35.47 dB/0.9390. Experimental results demonstrate that DCDiff outperforms LI, NMAR, DuDoNet, and InDuDoNet on both synthetic and clinical data. \
MICCAI, 2024. [[doi](https://doi.org/10.1007/978-3-031-72104-5_22)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://papers.miccai.org/miccai-2024/paper/1608_paper.pdf)

1. **UPGRADE-Net: Unsupervised Sinogram-domain Data-Consistent Network for Metal Artifact Reduction** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*Z. Wu et al.* \
**Summary** \
UPGRADE-Net tackles CT MAR directly in the sinogram by pairing a conditional denoising diffusion model (DDPM-MAR) with an unsupervised reverse pipeline that learns metal-trace statistics from pseudo masks injected into metal-free regions interpolated via LI. The diffusion model is regularized with two physics-driven constraints—conjugate-ray consistency to tie symmetric detector bins and accumulation-ray consistency to preserve per-view line integrals—so the network honors data fidelity without paired supervision. Trained on 40k synthetic cases assembled from DeepLesion slices (256×256, 361 views, 367 detectors) using 40 randomly sampled masks drawn from a library of 80 shapes (with the remaining 10 masks held out for 2k evaluation cases alongside real experimental scans), UPGRADE-Net lifts sinogram PSNR/SSIM to 33.44 dB / 0.961, surpassing FBP, LI, IMAR, SIDNN, Edge GAN, and matching supervised ACDNet while maintaining sharper lung and abdominal structures around large implants. \
IEEE Transactions on Medical Imaging, 2025. [[doi](https://doi.org/10.1109/TMI.2025.3630832)]

1. **UPMCL-Net: Unsupervised Projection-domain Multiview Constraint Learning for CBCT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*Z. Wu et al.* \
**Summary** \
UPMCL-Net directly learns to inpaint metal-corrupted CBCT projections without paired ground truth by combining a transformer-based MultiView Consistency Module (MVCM) that mines complementary anatomy across adjacent and interval views with a Hybrid Feature Attention Module (HFAM) that adaptively fuses intraview background cues and interview priors. Training is fully unsupervised: triangulation-based interpolation provides self-reconstruction targets, randomly generated pseudo masks expose the model to diverse missing patterns, and an adversarial loss sharpens fine textures in the synthesized metal traces. Trained on 25 of 30 intraoperative spine CBCT scans and evaluated on the remaining cases, UPMCL-Net surpasses LI, TRI, RegGAN, AOT-GAN, LBAM, and Palette on both simulated and clinical data. \
IEEE Transactions on Medical Imaging, 2025. [[doi](https://doi.org/10.1109/TMI.2025.3638630)]

1. **Dual-Domain Denoising Diffusion Probabilistic Model for Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*W. Xia et al.* \
**Summary** \
This article proposes a supervised dual-domain Denoising Diffusion Probabilistic Model (DDPM) for CT MAR, operating in both image and sinogram domains. The initial input for the sinogram domain DDPM model is generated by first applying a linear interpolation to the metal-corrupted sinogram data, reconstructing it, refining it with a trained FBPConvNet, and forward-projecting it. After obtaining such initially corrected sinogram, the DDPM is trained. At each iteration of DDPM, the non-metal area is inserted into the model's output to refine only the metal area. After reconstruction, another DDPM is trained to refine the image domain. Instead of predicting the noise, the image domain DDPM predicts the residual between the input image and the metal-free image. To reduce high computational cost during the inference, denoising diffusion implicit model (DDIM) is used. The models are trained on the simulated data from CT-MAR AAPM Grand Challenge dataset. For the sinogram-domain DDPM, the sinogram was resized to 512x512 for processing and resized back to the original size of 1000x900 after processing. On the other hand, the image-domain DDPM operates on the original image size of 512x512 pixels. The results are compared with linear interpolation, NMAR, and DICDNet on simulated and one clinical dataset. On a Tesla V100 GPU, the proposed method takes about 29 seconds to process a single image, including the time for the sinogram and image domain DDIMs. \
IEEE Transactions on Radiation and Plasma Medical Sciences, 2025. [[doi](https://doi.org/10.1109/trpms.2025.3582528)]

1. **PRAISE-Net: Deep Projection-domain
Data-consistent Learning Network for CBCT Metal
Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*Z. Wu et al.* \
**Summary** \
PRAISE-Net is a deep learning framework for metal artifact reduction (MAR) in cone-beam computed tomography (CBCT), designed to ensure data consistency in the projection domain. It employs a Low2High strategy consisting of two stages. In the first stage, the Prior Information-Guided Denoising Diffusion Probabilistic Model (PIG-DDPM) performs low-resolution inpainting of metal-corrupted regions. The inputs to this model are the projection data affected by metal artifacts and a prior estimate generated using linear interpolation; the target is the corresponding metal-free projection data. In the second stage, a Super-Resolution Reconstruction (SRR) module based on a Swin Transformer takes the inpainted low-resolution projections and reconstructs high-resolution projections. The target for this stage is the high-resolution metal-free projection data. To improve generalization to real clinical CBCT data, PRAISE-Net incorporates a CBCT Domain Adaptation (CBCT-DA) module into the training phase of PIG-DDPM. CBCT-DA uses two encoders to disentangle domain-invariant anatomical features from domain-specific features. It employs adversarial loss through a domain discriminator to align the feature distributions of simulated and clinical data, and uses mutual information loss to enforce separation between anatomical and domain-relevant representations. Importantly, CBCT-DA is used only during training to enable domain-invariant feature learning; it is not required during testing, where the trained model can be directly applied to clinical CBCT data. After high-resolution projections are restored, the final CBCT images are reconstructed using the Feldkamp-Davis-Kress (FDK) algorithm. \
IEEE Transactions on Instrumentation and Measurement, 2025. [[doi](https://doi.org/10.1109/TIM.2025.3551446)]

1. **Radiologist-in-the-Loop Self-Training for Generalizable CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Self-Supervised-blue.svg" alt="Self-Supervised"> \
*C. Ma et al.* \
**Summary** \
RISE-MAR is a novel radiologist-in-the-loop self-training framework for generalizable CT metal artifact reduction (MAR), addressing the domain gap and confirmation bias issues in existing supervised and semi-supervised MAR methods. The framework integrates a Clinical Quality Assessor (CQA)—a transformer-based network trained on radiologist-annotated CT images—that evaluates MAR outputs and ensures only high-quality pseudo ground-truths are used for training. CQA employs a spatial-frequency token mixer combining convolutional and self-attention mechanisms for multi-scale feature extraction, followed by a vectorization layer and quality head that predicts clinical quality scores. The self-training framework utilizes a teacher-student architecture, where the teacher network, pretrained on simulated MAR data, generates pseudo ground-truths for real clinical images. These pseudo ground-truths are assessed by CQA, and only high-quality ones (within a defined threshold) train the student MAR network via a supervised loss (L_sim) on paired simulated data and an unsupervised loss on real clinical images. The teacher model is progressively updated using an Exponential Moving Average (EMA) of the student’s improved weights, ensuring continuous refinement of pseudo ground-truths. Extensive experiments on multiple clinical datasets demonstrate RISE-MAR's superior generalization over state-of-the-art methods in out-of-domain datasets.\
[[arXiv](https://arxiv.org/pdf/2501.15610)]. IEEE TMI, 2025. [[doi](https://ieeexplore.ieee.org/abstract/document/10857416)]

1. **Implicit neural representation-based method for metal-induced beam hardening artifact reduction in X-rayCT imaging** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*H. S. Park et al.* \
**Summary** \
This paper introduces a parameter-free metal-induced beam hardening correction (MBHC) method for CT imaging utilizing implicit neural representations (INRs). In contrast to the MBHC approach, the proposed method generates two tomographic images: one representing the monochromatic attenuation distribution and the other capturing nonlinear beam hardening effects. This eliminates the need for explicit metal segmentation and parameter estimation. The INR-generated images are combined to simulate forward projection data, which is then used to compute a loss function for network training. Experimental results demonstrate effective reduction of beam hardening artifacts arising from interactions between metals, bone, and teeth. Furthermore, the method exhibits potential for addressing challenges associated with photon starvation and truncated fields-of-view, leading to improved image reconstruction quality. \
Medical Physics, 2025. [[doi](doi/10.1002/mp.17649)]

1. **Metal implant segmentation in CT images based on diffusion model** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*K. Xie et al.* \
**Summary** \
The authors propose to use a diffusion model "DiffSeg" to segment the metal implants in CT images. The method is compared against U-Net, Attention U-Net, R2U-Net, and DeepLabV3+. The training and testing is conducted using simulated data. Additionally, the method is also tested on real clinical data. The method is shown to outperform the other methods. Better segmentation of metals is shown to reduce the artifacts when using NMAR. \
BMC Medical Imaging, 2024. [[doi]( https://doi.org/10.1186/s12880-024-01379-1)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://bmcmedimaging.biomedcentral.com/articles/10.1186/s12880-024-01379-1) 
 
1. **A denoising diffusion probabilistic model for metal artifact reduction in CT**  <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*G. M. Karageorgos et al.* \
**Summary** \
The authors propose to use a denoising diffusion probabilistic model (DDPM) for metal artifact reduction in CT. The model was used to inpaint the metal traces in the projection domain. The training was done in the unsupervised manner but during the inference, the segmented metal trace was required. The DDPM model was compared with a Partial Convolutions-based U-Net and a Gated Convolutions-based GAN. The proposed method showed promising results, however, the method was computationally expensive. Moreover, it was less effective in reducing metal artifacts caused by large metal objects. The authors also noted that it is crucial to have an accurate metal trace segmentation for the best performance of the proposed network\
IEEE TMI, 2024. [[doi]](https://ieeexplore.ieee.org/document/10586949)

1. **DiffMAR: A Generalized Diffusion Model for Metal Artifact Reduction in CT images**  <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*T. Cai et al.* \
**Summary** \
DiffMAR frames CT metal artifact reduction as reversing a physically motivated *linear degradation* process that mimics metal-artifact formation, and learns an iterative restoration path with a conditional diffusion model trained on paired metal-corrupted/metal-free CT images. To reduce error accumulation across the multi-step reverse process, it introduces a Time-Latent Adjustment (TLA) module that adaptively adjusts time embeddings at the latent level. To better preserve anatomy (and avoid over-smoothing), it adds a Structure Information Extraction (SIE) module that extracts structural priors from the linearly-interpolated (LI) correction in the image domain and injects them at each restoration step. On a DeepLesion-based synthetic benchmark (1,000 training images; 90 training masks; 2,000 test images; images resized to 416×416 with 640 views) and clinical testing (SpineWeb plus an in-house dental CT set), DiffMAR outperforms LI, DuDoNet, InDuDoNet(+), CycleGAN/Pix2PixGAN, and DICDNet/ACDNet, improving average PSNR/SSIM from 45.31 dB / 0.9899 (ACDNet) to 46.28 dB / 0.9922 and lowering RMSE from 0.0133 to 0.0119 on the synthetic test set. \
IEEE Journal of Biomedical and Health Informatics, 2024. [[doi](https://doi.org/10.1109/JBHI.2024.3439729)]

1. **PND-Net: Physics-inspired Non-local Dual-domain Network for Metal Artifact Reduction**  <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
 *J. Xia et al.* \
 **Summary**\
 The authors propose to use three networks. One is called non-local sinogram decomposition network (NSD-Net) to acquire the weighted artifact component in sinogram domain. The second network is an image restoration network (IR-Net) to reduce the residual and secondary artifacts in the image domain. The third trainable fusion network (F-Net) in the artifact synthesis path is used for unpaired learning. The method is compared with linear interpolation, NMAR, DuDoNet, DuDoNet++, InDuDoNet+, ADN, and U-DuDoNet.\
 IEEE TMI, 2024. [[doi](https://ieeexplore.ieee.org/document/10404006)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/Ballbo5354/PND-Net/tree/main)


1. **Dual Domain Diffusion Guidance for 3D CBCT Metal Artifact Reduction**  <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*Y. Choi et al.* \
**Summary** \
The authors propose to use two 2D diffusion models in image domain to synthesize metal artifacts and metal free images, respectively. The combination of metal artifact and metal free images is forward projected and a guidance from the error in the projection domain is used. The results are compared with InDuDoNet+, ACDNet, FEL, and Blind DPS methods.  \
WACV, 2024. [[doi](https://openaccess.thecvf.com/content/WACV2024/papers/Choi_Dual_Domain_Diffusion_Guidance_for_3D_CBCT_Metal_Artifact_Reduction_WACV_2024_paper.pdf)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://openaccess.thecvf.com/content/WACV2024/papers/Choi_Dual_Domain_Diffusion_Guidance_for_3D_CBCT_Metal_Artifact_Reduction_WACV_2024_paper.pdf)

1. **InDuDoNet+: A Deep Unfolding Dual Domain Network for Metal Artifact Reduction in CT Images** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*H. Wang, Y. Li, H. Zhang, D. Meng, Y. Zheng* \
**Summary** \
InDuDoNet+ is the journal-extension of InDuDoNet that keeps the 10-stage dual-domain deep-unfolding backbone (alternating S̃-net and X-net solved by a proximal-gradient unfolding of the joint-regularised problem min ‖PX−Ỹ⊙S̃‖²_F + α‖(1−T_r)⊙(Ỹ⊙S̃−Y)‖²_F + λ₁g₁(S̃) + λ₂g₂(X)) but replaces the original heuristic U-shape Prior-net with a **knowledge-driven Prior-net** that embeds CT-physics priors into the normalisation coefficient Ỹ, substantially improving interpretability, parameter efficiency and cross-dataset generalisation. The new Prior-net works in two steps: (i) a classical thresholding/k-means clustering pipeline on the LI-corrected image X_LI produces a coarse tissue segmentation X̃_c by forcing air → −1000 HU, soft tissue → 0 HU and bone → original values (metal value arbitrary since only sinogram bins close to — not inside — the metal trace contribute after normalisation); (ii) a shallow **WNet** of only three Conv+BN+ReLU layers then refines X̃_c pixel-wise into the final prior image X̃, from which Ỹ = P X̃ is obtained by forward projection. In parallel the paper introduces a **channel-wise concatenation/detachment** mechanism: each proxNet at stage n receives not just the single-channel update Ŝ_{n−1} (or X̂_{n−1}) but also an auxiliary variable Q^s_{n−1} (or Q^x_{n−1}) of dimension N_s (or N_x), set to 32, expanding the input to N_b×N_p×(1+N_s) and letting the proximal networks propagate richer iteration-wise information; the proxNets themselves are four [Conv + BN + ReLU + Conv + BN + Skip] residual blocks with 3×3 kernels. Initial states S̃₀, X₀, Q^s₀, Q^x₀ are produced by ResNet-structured proxNet_{θ^(0)} modules applied to the LI inputs together with learnable 3×3×32 convolutional filters. Training uses the same staged MSE loss as InDuDoNet (β_N=1, β_n=0.1 for n<N, γ=0.1). Data: synthesised DeepLesion with fan-beam geometry, polychromatic X-ray, partial-volume effect, beam hardening and Poisson noise — 1,000 clean CT × 90 metal masks for training, 200 CT from 12 patients × 10 metal masks for test (2,000 pairs, 416×416 images, 640 views, 641×640 sinograms); a cross-body-site Synthesised-Dental set; and clinical SpineWeb vertebrae images for visual evaluation (2,500 HU threshold for clinical metal masks). On the synthesised DeepLesion benchmark (by metal size, Large → Small) InDuDoNet+ reports PSNR/SSIM of 36.28/0.9736, 39.23/0.9872, 41.81/0.9937, 45.03/0.9952, 45.15/0.9959 — average **41.50 dB / 0.9891**, with the lowest average RMSE 16.93 HU among all compared methods: Input 27.06/0.7586/73.33 HU, LI 29.27/0.9347/53.37 HU, NMAR 29.48/0.9442/50.08 HU, CNNMAR 30.38/0.9644/45.69 HU, DuDoNet 31.14/0.9814/43.96 HU, DSCMAR 33.21/0.9375/32.16 HU, DuDoNet++ 39.69/0.9886/27.78 HU, original InDuDoNet 41.48/0.9904/18.20 HU. Although the PSNR/SSIM numbers are essentially tied with InDuDoNet, the standard deviation of PSNR drops from 4.73 to 4.37 dB and the standard deviation of SSIM from 0.0088 to 0.0066, i.e. InDuDoNet+ is **more stable across metal sizes**; the knowledge-driven Prior-net is also lighter (the WNet has only 3 convolutional layers, vs the 4-level U-shape in InDuDoNet), which the authors argue reduces overfitting and improves cross-dataset generalisation to Synthesised-Dental and clinical SpineWeb. An ablation with "InDuDoNet w/o Prior-net" (the unfolding without normalisation) gives 40.18/0.9876 average PSNR/SSIM at only 1.74 M parameters, validating that most of the gain beyond DuDoNet++ comes from the physics-geometry unfolding itself rather than the Prior-net alone, while the Prior-net is responsible for the remaining stability margin on large implants. The authors note several limitations: (1) evaluation on clinical data is qualitative only, since no paired artifact-free ground truth exists; (2) clinical metal masks at inference still rely on a simple 2,500-HU threshold, which can propagate segmentation errors; (3) the hand-crafted thresholding step inside the knowledge-driven Prior-net can be inaccurate for atypical tissue distributions, and the WNet refinement is only three convolutional layers, so it may not fully correct such errors; (4) as in InDuDoNet, the unfolding depth is capped at N=10 because deeper unfolding invites gradient vanishing. \
[[arXiv](https://arxiv.org/abs/2112.12660)] Medical Image Analysis, 2023. [[doi](https://doi.org/10.1016/j.media.2023.102729)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/hongwang01/InDuDoNet_plus)

1. **Polyner: Unsupervised Polychromatic Neural Representation for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*Q. Wu et al.* \
**Summary** \
Polyner treats MAR as a nonlinear reconstruction problem, embedding a differentiable polychromatic CT forward model and an energy-dependent smoothness prior inside an implicit neural representation (hash-encoded MLP) so each scan can be optimized end-to-end without external training data. During inference, 80 rays are randomly sampled per iteration and the network learns case-specific attenuation fields across up to 101 energy bins in ~2 minutes for a 256×256 slice, preserving metal-trace signals instead of masking them. On synthetic DeepLesion data it reaches 37.57 dB / 0.975 SSIM (2nd overall) and on the OOD XCOM benchmark it tops all baselines at 38.74 dB / 0.951 SSIM, outperforming supervised CNN-MAR/DICDNet/ACDNet. The same per-case optimization transfers to real micro-CT walnuts with clips and mouse tibia scans, removing residual streaks left by FBP, LI, Score-MAR, and even ACDNet. \
NeurIPS, 2023. [[doi](https://dl.acm.org/doi/abs/10.5555/3666122.3669170)][![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/iwuqing/Polyner)[![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://proceedings.neurips.cc/paper_files/paper/2023/file/dbf02b21d77409a2db30e56866a8ab3a-Paper-Conference.pdf)

1. **Deep learning based projection domain metal segmentation for metal artifact reduction in cone beam computed tomography** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*H. Agrawal et al.* \
**Summary** \
The authors proposed to use noisy Monte Carlo simulations to train a U-Net for metal segmentation in the projection domain. Including both the crops and full size images in the training data improved the metal segmentation performance. The evaluations were conducted on real clinical CBCT data to show the reduction in metal artifacts after inpainting the segmented metal traces in the projections. The evaluations included challenging datasets with large high density objects, motion artifacts, and out of field of view metals. \
IEEE Access, 2023. [[doi](https://ieeexplore.ieee.org/document/10250444)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://ieeexplore.ieee.org/document/10250444)

1. **Quad-Net: Quad-domain Network for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*Z. Li et al.* \
**Summary** \
In addition to dual-domain artifact reduction in the sinogram and image domains, the paper also proposes applying Fourier domain processing to features in both domains.\
[[arXiv](https://arxiv.org/abs/2207.11678)]. IEEE TMI, 2024. [[doi](https://ieeexplore.ieee.org/document/10385220)]. [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/longzilicart/Quad-Net/tree/master)


1. **Unsupervised metal artifacts reduction network for CT images based on efficient transformer** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*L. Zhu et al.*\
**Summary**
A method to incorporate efficient Transformer blocks in the two generators of the CycleGAN. Additionally, the method also includes a loss term for the forward projections in the non-metal area. The results are compared with linear interpolation, CycleGAN and ADN.\
BSPC, 2023. [[doi](https://doi.org/10.1016/j.bspc.2023.105753)]

1. **Simulation-driven training of vision transformers enables metal artifact reduction of highly truncated CBCT scans** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*F. Fan et al.*  \
**Summary** \
The paper proposes to use Swin-transformer for metal segmentation in the projection domain for CBCT data. Simulation data is used for the training. U-Net is shown to work best when the test data is similiar to the train data. But when the test data source is different than training, Swin transformer performs better.\
Medical Physics, 2023. [[doi](https://aapm.onlinelibrary.wiley.com/doi/full/10.1002/mp.16919)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://aapm.onlinelibrary.wiley.com/doi/full/10.1002/mp.16919)

1. **OSCNet: Orientation-Shared Convolution Representation for CT Metal Artifact Learning** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*H. Wang, Q. Xie, Y. Li, Y. Huang, D. Meng, Y. Zheng* \
**Summary** \
OSCNet is a supervised, image-domain deep-unfolding MAR network whose central novelty is an **orientation-shared convolutional representation** that explicitly bakes in the rotational symmetry of metal streaking patterns, so the same learnable filter is re-used across discrete orientations rather than re-estimated from data at each angle. The method extends the DICDNet / ACDNet convolutional-dictionary backbone by replacing the unconstrained 2-D convolution kernels with **Fourier-series-expansion-based filter parameterisation**: each base kernel is written as C_k(θ_l) = Σ_{t=1..p} [a_qtk · cos(·) + b_qtk · sin(·)], where a_qtk and b_qtk are the parameters learned from training data, θ_l ∈ {0°, 45°, 90°, …} are the shared orientations, and L (set to 8) orientations are used per filter bank. At test time the network generates orientation-rotated copies of the same learned base filter, so the effective representation capacity for streak patterns is L × K (K=4 base kernels), while the number of free parameters scales with p×K (p=9) instead of L×K — this is the source of OSCNet's claimed generalisation advantage on unseen body sites. A proximal-gradient optimisation over the artifact-free image X and feature map M is unrolled into N=10 iterative stages, each containing an M-subnet that updates the feature maps and an X-subnet that updates the image; the mesh size h used in the Fourier-series integration is set to 1/4, and the ResNet-style proxNets inherit the 4-residual-block structure of DICDNet. The training loss is the same DICDNet-style multi-term loss used across this line of work — stage-wise Frobenius reconstruction plus L1 sparsity and artifact-consistency penalties with stage weighting μ_T=1 and μ_t<T=0.1. Training is performed in PyTorch on an NVIDIA Tesla V100-SMX2 GPU with the Adam optimiser, batch size 16, patch size 64×64, an initial learning rate halved every 30 epochs, and 200 total epochs. Data follows the DSCMAR / DICDNet protocol on DeepLesion: 1,200 clean abdomen/thorax CT × 90 training metal masks, 200 test CT × 10 test metal masks (metal sizes [2061, 890, 881, 451, 254, 124, 118, 112, 53, 35] pixels, grouped into 5 buckets), all at 416×416 with 640 uniformly sampled projections in [0°, 360°]. Cross-body-site generalisation is evaluated on a Synthesised Dental test set (same simulation protocol on dental CTs) and clinical performance on CLINIC-metal with a 2,500-HU threshold for metal masks. On the synthesised DeepLesion test (PSNR/SSIM by metal size, Large → Small): OSCNet reports 30.60/0.9786, 33.10/0.9362, 39.32/0.9893, 45.04/0.9958, 45.45/0.9962 — average **42.19 dB / 0.9931**, beating Input (27.06/0.7586), LI (29.27/0.9347), NMAR (29.48/0.9442), CNNMAR (30.38/0.9644), DuDoNet (31.14/0.9814), DSCMAR (33.21/0.9375), InDuDoNet (41.48/0.9897) and DICDNet (41.83/0.9923). The qualitative Synthesised-Dental generalisation test (OSCNet 42.28/0.9924) and the CLINIC-metal real-data visual comparison both show that the orientation-shared parameterisation better preserves bone detail than its non-rotation-aware predecessors on out-of-training-distribution images — consistent with the paper's argument that explicitly encoding the rotational prior improves transfer. The authors also publish an extended variant, **OSCNet+**, that adds a dynamic-convolution sub-network on top of the orientation-shared base to make the representation adaptive to per-image artifact patterns, further improving generalisation (reported in the journal extension of this work). Limitations noted or implied in the paper: (1) the orientation set is discretised to L=8 — angles between the sampled orientations rely on Fourier interpolation and may handle highly irregular / non-symmetric metal clusters less cleanly; (2) clinical evaluation is qualitative only because CLINIC-metal has no paired ground truth; (3) like DICDNet / ACDNet, OSCNet is image-domain-only and does not use projection data, which is deliberate but caps the ceiling on what can be recovered for very-large-implant cases where the projection inpainting prior is strongest. \
[[arXiv](https://arxiv.org/abs/2212.13166)] MICCAI, 2022. [[doi](https://doi.org/10.1007/978-3-031-16446-0_63)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/hongwang01/OSCNet)

1. **ACDNet: Adaptive Convolutional Dictionary Network for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*H. Wang, Y. Li, D. Meng, Y. Zheng* \
**Summary** \
ACDNet is a supervised, image-domain deep-unfolding MAR network whose central contribution is an explicit **weighted convolutional dictionary (WCD)** model for the artifact term: given a metal-corrupted CT image Y = X + A (restricted to the non-metal mask I), the paper models the artifact as A = (D ∗ K) ⊗ M, where D is a sample-invariant convolutional dictionary that captures the generic streaking / star-shape patterns shared across patients, K is a sample-variant weighting tensor that adapts the representation kernel to the specific content of each input image, M is a feature map, ∗ denotes convolution and ⊗ denotes another 2-D convolution. Because K is inferred per-image the effective kernel D ∗ K is **adaptive** — unlike prior convolutional-dictionary MAR models that use a fixed kernel bank — so the network can tune its artifact representation to each scan. A maximum-a-posteriori objective over (X, K, M) is solved by a proximal-gradient algorithm and each of its iterative substeps is unrolled into a corresponding network module, giving T=10 stages with clear physical meaning: at every stage an M-subnet updates the feature map M^(t), a K-subnet produces the adaptive weights K^(t) (including adaptive step sizes {η_i}), and an X-subnet updates the artifact-free image X^(t). Only the common dictionary D, the proxNet weights, and the step sizes {η_i} are trained end-to-end; there is no sinogram-domain branch, so ACDNet operates purely in the image domain and can therefore be dropped into existing CAD pipelines that do not have projection data access. Training uses a three-term loss L = Σ_t μ_t ‖I⊙(X − X^(t))‖²_F + ω₁Σ_t μ_t ‖I⊙(X − X^(t))‖₁ + ω₂Σ_t μ_t ‖I⊙(Y − X^(t) − A^(t))‖₁ with μ_T = 1, μ_t = 0.1 for t < T, and ω₁ = ω₂ = 5×10⁻⁴. The network is trained with Adam (initial LR 2×10⁻⁴, halved at epochs {50, 100, 150, 200}) in PyTorch for 300 epochs on an NVIDIA Tesla V100-SMX2 GPU, batch size 32, input patches of 64×64 with random horizontal/vertical flip augmentation. Data follows the DSCMAR protocol on DeepLesion: 1,000 clean CT images × 90 metal masks for training, 200 test CT images × 10 metal masks for test (adjacent metal pairs grouped into 5 size buckets), with an additional Dental synthesised set used to assess cross-body-site generalisation and the CLINIC-metal dataset (HU > 2,500 threshold for clinical metal masks) used for real-image qualitative and downstream-segmentation evaluation. On the synthesised DeepLesion test set ACDNet achieves an average PSNR/SSIM of **40.68 dB / 0.9933**, the best across all compared methods: Input 27.06/0.7586, LI 29.27/0.9347, NMAR 29.48/0.9442, CNNMAR 30.38/0.9644, DuDoNet 31.14/0.9814, DSCMAR 33.21/0.9375 and DuDoNet++ 39.69/0.9886. What stands out is the parameter/speed profile: at only **1,602,809 parameters** ACDNet is about 16× smaller than DuDoNet / DSCMAR (25.83 M each) and DuDoNet++ (25.98 M), while its test time per image is 0.3138 s on V100 — faster than DuDoNet (0.4225 s), DSCMAR (0.3638 s) and DuDoNet++ (0.8062 s). On the cross-body-site Dental generalisation test (model trained on abdomen/thorax DeepLesion, tested on dental) ACDNet qualitatively preserves more bone detail and removes more artifacts than DuDoNet / DSCMAR / DuDoNet++ thanks to the explicit WCD regularisation, and on CLINIC-metal it produces visibly cleaner pelvic structures and leads to the best downstream multi-bone segmentation quality among the compared methods. The authors note several implicit limitations: (1) clinical evaluation is only qualitative / downstream-segmentation-based because the CLINIC-metal dataset has no paired ground truth, so quantitative clinical performance claims are indirect; (2) clinical metal masks still rely on a hard 2,500-HU threshold, so mask errors can propagate into the unfolding; (3) ACDNet is image-domain-only and therefore cannot exploit sinogram data even when it is available — a deliberate deployability trade-off rather than a design flaw. \
[[arXiv](https://arxiv.org/abs/2205.07471)] IJCAI, 2022. [[doi](https://doi.org/10.24963/ijcai.2022/201)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/hongwang01/ACDNet)

1. **Low-Dimensional Manifold Constrained Disentanglement Network for Metal Artifact Reduction (LDM-DN)** <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
*C. Niu, W. Cong, F. Fan, H. Shan, M. Li, J. Liang, G. Wang* \
**Summary** \
LDM-DN augments the Artifact Disentanglement Network (ADN) with a low-dimensional patch-manifold (LDM) constraint that forces the reconstructed artifact-free image to lie on a low-dimensional manifold of image patches, compensating for the weak structural supervision that pure adversarial unpaired MAR training provides. The base architecture reuses ADN's four-way encoder/decoder layout — two encoders E_I and E_I^{aa} extract clean and artifact-affected latent codes from a patch of size 1×256×256, and three decoders G_I / G_I^a produce artifact-corrected, artifact-affected and artifact-free outputs via the branches E_I^ca → G_I, E_I^{aa} → G_I^a and E_I → G_I — so the ADN adversarial + cycle-consistency losses carry over unchanged. On top of each encoder's latent code, LDM-DN adds two additional convolutional layers that project the Z_x feature (512×64×64, down-sampling rate 8 from the 256×256 input) down to Z_{xt} (64×64×64), and extracts 8×8 patches with a 128-dimensional descriptor. These patches form the point cloud whose manifold dimensionality is penalised inside the training loop via an LDM-DN algorithm that alternately (a) optimises the ADN-style synergistic losses and (b) solves the low-dimensional manifold model (LDMM) iteration on the current patch set. The authors further explore four training paradigms for the disentanglement backbone: unpaired-only (ADN / ADN*), paired-only (Sup: single encoder-decoder with skip connections), paired+LDM (LDM-Sup), and the hybrid **LDM-DN-Sup** that combines unpaired, paired and LDM losses together — intended to absorb both the diversity of clinical unpaired data and the pixel-level correctness of synthesised paired data while using LDM to prevent over-correction. The method is trained on two datasets: (i) a synthesised DeepLesion set with 3,918 training pairs and 200 test pairs (CNNMAR simulation protocol, the same split as ADN); and (ii) a clinical set comprising 6,170 metal-affected + 21,190 metal-free CT images for unpaired training and 100 metal-affected images for visual evaluation, with the artifact/clean split driven by HU-histogram criteria (≥ 400 pixels at HU > 2,500 → artifact-affected; maximum HU < 2,000 → artifact-free), plus 6,170 CatSim-generated paired images for the hybrid scheme. Implementation is in PyTorch, batch size 1, LDM weight µ=0.6, all other hyperparameters kept identical to ADN. On the DeepLesion test set (PSNR / SSIM): unpaired methods — CycleGAN 30.8 / 72.9, DIP 26.4 / 75.9, MUNIT 14.9 / 7.5, DRIT 25.6 / 79.7, ADN 33.6 / 92.4, ADN* (authors' retrained version that picks the best epoch) 34.0 / 92.9, and **LDM-DN 35.0 / 94.2** — a +1.0 dB / +1.3 pt gain over ADN*. Paired methods — CNNMAR 32.5 / 91.4, U-Net 34.8 / 93.1, cGANMAR 34.1 / 93.4, Sup (bare encoder-decoder) 37.6 / 96.1, **LDM-Sup 38.0 / 96.3** — showing that the LDM constraint also lifts fully-supervised training. A sensitivity sweep over the unpaired ratio (ADN-0.85 / 0.50 / 0.15 of artifact-affected images) shows that ADN is effectively insensitive to this ratio (PSNR 34.1 / 34.0 / 34.0, SSIM 92.8 / 92.8 / 92.9), so the 0.15-ratio (closer to clinical reality) is used as the standard baseline. On real clinical data no numerical ground truth exists, so evaluation is purely qualitative; the hybrid LDM-DN-Sup preserves local structural details better than ADN* (visible in the green/blue ROIs) and avoids the over-correction observed when ADN and Sup are naively combined (ADN-Sup produces over-sharp boundaries and over-dark regions). The authors explicitly discuss several limitations: (1) the unpaired learning objective does not strictly converge, so the final model is selected by picking the best epoch on the synthesised validation set — a pragmatic but brittle model-selection choice; (2) models trained only on synthesised data perform worse on clinical images than unpaired clinical models, confirming a persistent simulated-to-clinical domain gap; (3) the hand-crafted HU criteria that split "artifact-affected" vs "artifact-free" clinical images can mis-route borderline cases; (4) LDM alone cannot avoid all over-correction — the hybrid scheme is needed to balance cycle-consistency, paired fidelity and manifold regularity; (5) synthesised test metrics (PSNR/SSIM) improve monotonically with more supervision, but the clinical qualitative results reveal that "best synthesized score" does not always translate to best clinical image, motivating the need for the hybrid training. \
[[arXiv](https://arxiv.org/abs/2007.03882)] IEEE Transactions on Radiation and Plasma Medical Sciences, 2022. [[doi](https://doi.org/10.1109/TRPMS.2021.3122803)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://pmc.ncbi.nlm.nih.gov/articles/PMC9295822/)

1. **DL-based inpainting for metal artifact reduction for cone beam CT using metal path length information** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*T. M. Gottschalk et al.* \
**Summary** \
The authors propose to use a U-Net for inpainting the metal traces in the projection domain. The metal path length information is used to guide the inpainting along with the metal mask. Authors suggest that it is beneficial to not discard the information inside the metal mask. Additionally, 10 nearby projections were also concatenated to the input, making the total number of channels in the input 13. The output was the projection to be inpainted/corrected.\
Medical Physics, 2022. [[doi](https://aapm.onlinelibrary.wiley.com/doi/10.1002/mp.15909)]

1. **A fidelity-embedded learning for metal artifact reduction in dental CBCT** <img src="https://img.shields.io/badge/Iterative-blue.svg" alt="Iterative"> \
*H. S. Park et al.* \
**Summary** \
The authors propose using an iterative method for fidelity-embedded learning to reduce metal artifacts in dental CBCT images. In each iteration, the network learns to predict the remaining metal artifact within the image. Additionally, a data-fidelity error term is calculated in the projection domain, but only for the non-metal areas. The Astra Toolbox was utilized for implementation. Training was conducted on simulated data, whereas testing was performed on both simulated and real clinical data. \
Medical Physics, 2022. [[doi](https://doi.org/10.1002/mp.15720)] 


1. **InDuDoNet: An Interpretable Dual Domain Network for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*H. Wang, Y. Li, H. Zhang, J. Chen, K. Ma, D. Meng, Y. Zheng* \
**Summary** \
InDuDoNet is a supervised deep-unfolding dual-domain MAR network that explicitly bakes the CT imaging geometry into the architecture so that each layer corresponds to a specific operation in an iterative optimisation algorithm, sacrificing none of the data-driven prior fitting of CNNs while recovering model interpretability that pure black-box MAR networks lack. The method formulates MAR as a joint sinogram / image reconstruction problem: given the metal-affected sinogram Y, binary metal trace T_r, a Radon operator P and a normalisation coefficient Ỹ (forward projection of a prior image X̃), the clean sinogram is written as S = Ỹ⊙S̃ and the objective becomes min_{S̃,X} ‖PX − Ỹ⊙S̃‖²_F + α‖(1−T_r)⊙(Ỹ⊙S̃ − Y)‖²_F + λ₁g₁(S̃) + λ₂g₂(X); the prior regularisers g₁ and g₂ are learned implicitly via CNN-based proximal operators rather than hand-crafted. A proximal-gradient algorithm that uses only simple operations (point-wise multiplication, forward/backward projection) alternately updates S̃ and X; N=10 iterations of this algorithm are unfolded into N=10 stages of the network, each stage containing an S̃-net (computes Ŝ_{n-1} per Eq. 7 and passes it through proxNet_{θ_s̃^(n)}) and an X-net (computes X̂_{n-1} per Eq. 9 and passes it through proxNet_{θ_x^(n)}). Both proxNets share the same structure — four [Conv + BN + ReLU + Conv + BN + Skip] residual blocks. A separate U-shape Prior-net is trained to generate the normalisation coefficient Ỹ from the concatenation of the metal-affected image X_ma and its LI-corrected counterpart X_LI; X₀ is initialised by passing X_LI through a dedicated proximal network proxNet_{θ_x^(0)}. Training uses a staged MSE loss L = Σₙ βₙ‖(X_n − X_gt)⊙(1−M)‖²_F + γΣₙ βₙ‖Ỹ⊙S̃_n − Y_gt‖²_F with β_N=1, β_n=0.1 for n<N, and γ=0.1, supervising each stage while emphasising the final output. Data follows the DeepLesion-based synthesis protocol from DSCMAR: 1,000 CT images × 90 metal masks at training, 200 CT images from 12 patients × 10 metal masks at test time (2,000 test pairs), all resized to 416×416 pixels with 640 uniform views and sinograms of size 641×640; the 10 test metal implants span 35–2,061 pixels and are grouped into five size buckets. Implementation uses PyTorch with the ODL library for differentiable Radon operators on an NVIDIA Tesla V100-SMX2 GPU; Adam (β₁=0.5, β₂=0.999), LR 2×10⁻⁴ halved every 40 epochs, 100 epochs total, batch size 1, each iteration randomly pairs an image with a metal mask. Clinical generalisation is assessed on CLINIC-metal (14 volumes labelled for sacrum / left hip / right hip / lumbar spine) via downstream multi-bone segmentation with a U-Net trained on the metal-free CLINIC data. Quantitative results on the synthetic test set by metal size (Large → Small) are PSNR/SSIM of 36.74/0.9801, 39.32/0.9896, 41.86/0.9931, 44.47/0.9942, 45.01/0.9948 — average **41.48 dB / 0.9904**, beating Input 27.06/0.7586, LI 29.27/0.9347, NMAR 29.48/0.9442, CNNMAR 30.38/0.9644, DuDoNet 31.14/0.9814, DSCMAR 33.21/0.9375, and DuDoNet++ 39.69/0.9886. The ablation over stage count shows that N=0 (X_LI only) gives 33.54/0.9521, N=6 gives 41.00/0.9899, N=10 is best at 41.48/0.9904, and N=12 actually dips slightly (41.64 PSNR but 0.9897 SSIM) because the deeper unfolding starts suffering from gradient vanishing; the authors pick N=10 for the PSNR/SSIM/parameter sweet spot. At N=10 the full network has only 5.17 M parameters — fewer than DSCMAR (25.83 M) and DuDoNet++ (25.98 M) — and takes 0.5116 s per 416×416 image on V100 (N=6 takes 0.3526 s with 4.54 M parameters). On the CLINIC-metal downstream segmentation task, InDuDoNet raises the average Dice across sacrum / left hip / right hip / lumbar spine to 0.9490 vs 0.9478 (DuDoNet++), 0.9469 (DuDoNet), 0.9396 (DSCMAR), 0.9367 (CNNMAR), 0.9303 (NMAR), 0.9263 (LI) and 0.9245 (raw input). The authors' own limitations noted in the paper: (1) N=12 underperforms N=10 due to gradient vanishing, so deeper unfolding past 10 stages is not beneficial without additional stabilisation; (2) clinical metal masks at inference are produced by simple HU-thresholding at 2,500 HU, so segmentation errors on the metal mask can propagate into the MAR result; (3) all training data are synthetic DeepLesion pairs, leaving a simulated-to-clinical domain gap that is only indirectly validated through the pelvic-segmentation downstream task rather than with paired clinical ground truth. \
[[arXiv](https://arxiv.org/abs/2109.05298)] MICCAI, 2021. [[doi](https://doi.org/10.1007/978-3-030-87231-1_11)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/hongwang01/InDuDoNet)

1. **Metal Artifact Reduction In Cone-Beam Extremity Images Using Gated Convolutions** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*H. Agrawal et al.* \
**Summary** \
The authors propose to use Gated Convolutions for the metal area inpainting in the projection domain. The method is compared against linear interpolation, U-Net, and Partial Convolutions. \
ISBI, 2021. [[doi](https://ieeexplore.ieee.org/document/9434163)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://acris.aalto.fi/ws/portalfiles/portal/64983388/ELEC_Agrawal_etal_Metal_artifact_reduction_IEEE_ISBI2021_acceptedauthormanuscript.pdf)

1. **Multiple Window Learning for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*C. Niu et al.* \
**Summary** \
The authours proposed a deep learning-based framework to enhance CT MAR by leveraging information across multiple Hounsfield Unit (HU) windows. They argued that traditional approaches typically normalize CT data into a single HU window, which can compromise training effectiveness due to unequal emphasis on different HU ranges—an important consideration in clinical applications where tissues are best visualized under specific window settings.  Multiple Window Learning Network (MWLNet) addresses this by employing multiple convolutional neural network (CNN) branches, each tailored to a specific HU window. Inter-branch communication is facilitated by Window Transfer (WT) layers, which normalize and clip image data to map across HU ranges, enabling end-to-end training with gradients flowing between branches to enforce learning across scales. The training strategy uses an L1 loss summed across all window branches to ensure balanced learning, with three specific windows selected: [-1000, 2000], [-320, 480], and [-160, 240], though the method is flexible to other configurations. Training utilized paired artifact-free and artifact-affected images generated via CatSim-based simulation. MWLNet was evaluated on simulated CT scans of spine, teeth, and hip regions, as well as clinical datasets, with all images being 512×512 slices exhibiting varying artifact levels. Results showed that MWLNet outperformed NMAR and single-window CNN baselines (SWLNets) in PSNR, SSIM, and visual quality, notably preserving fine structures in narrow windows while reducing artifacts more effectively. Intermediate results validated that contextual information from larger HU windows improves prediction accuracy in smaller windows.
Proc. SPIE, 2021. [[doi]( https://doi.org/10.1117/12.2596239)]



1. **Deep-MAR: Fast Enhanced CT Metal Artifact Reduction Using Data Domain Deep Learning** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*M. U. Ghani, W. C. Karl* \
**Summary** \
Deep-MAR formulates CT metal artifact reduction as **non-blind sinogram completion** and trains a conditional GAN (CGAN) to inpaint the metal trace directly in the projection domain — the completed sinogram is then reconstructed with conventional filtered back-projection (FBP), making the method a drop-in pre-FBP module that fits into existing scanner workflows without modifying the reconstruction pipeline. The generator G is an encoder–decoder CNN (with BatchNorm, leaky-ReLU in the down-path, ReLU + transposed-convolutions in the up-path, dropout after every BN layer in the final block to limit overfitting) that takes a metal-trace-masked input sinogram and outputs only the correction inside the metal region via the residual form G(x) = x + M ⊙ x_D1, where M is the binary metal mask and x_D1 is the last-layer feature: this preserves non-metal sinogram samples exactly and focuses the capacity on the missing data. The discriminator D operates on the **full sinogram** (not patch-based) because metal-corrupted missing-data regions follow a coherent sinusoidal geometry that local patches cannot capture; for speed, D runs on 2×-downsampled sinograms. The training objective combines a standard CGAN adversarial loss (weighted by λ=10) with an L2 reconstruction term on the completed sinogram. To handle the data scarcity of the security-CT application that motivated the work, the authors build a two-tier training regime with **transfer learning**: (1) a large simulated corpus is generated via a physically-accurate Imatron-C300-style X-ray simulator (poly-energetic source with N=121 uniformly sampled energies between 10–130 keV, detector response g^(i)=2.6×10⁻³·E^(i) pA/quanta, source intensity I₀=1.7×10⁵ photons/ray, electronic noise variance σ_e²=3.37 pA², Beer-Lambert line integrals with NIST XCOM attenuation coefficients, parallel-beam 720 views × 1024 detector channels zero-padded to 1024×768 sinograms, 475×475 mm² FOV) coupled with a **stochastic bag simulator** that randomly places up to 5 metallic / non-metallic objects per scene; this yields 10,000 base scenes × (up to 5 metals/scene) = 50,000 simulated sinogram pairs plus 25 hand-crafted cylinder/sheet "suitcase" scenes; (2) a smaller real set of 1,706 metal-free slices from a real Imatron C300 is augmented by virtually deleting up to 5 metal regions per scene, yielding 8,530 real training pairs used for transfer-learning fine-tuning. Horizontal-flip augmentation is used on both sets, the ASTRA toolbox handles the forward projections, and test sets comprise 510 simulated pairs (102 scenes × up to 5 metal layouts) plus 159 real pairs from ALERT Task Order 4 for quantitative evaluation. Optimisation uses Adam (LR 2×10⁻⁴, β₁=0.9, β₂=0.999) with mini-batch size 6 over 25 epochs on the simulated set, and in the first 4 epochs the discriminator takes 6−k extra steps per generator update (k=1..4) to stabilise training. On the 510 simulated test images Deep-MAR achieves **MSE 1.2×10⁻⁴, SSIM 0.9941, PSNR 60.32 dB (units cm⁻¹)**, vs Uncorrected 3.9×10⁻² / 0.7205 / 33.72 dB, LI-MAR 4.7×10⁻⁴ / 0.9750 / 54.67 dB, and WNN-MAR 5.5×10⁻⁴ / 0.9713 / 53.81 dB — a 74% MSE reduction over LI-MAR and 78% over WNN-MAR. On the 159 real test pairs the transfer-learning model reaches MSE 5.8×10⁻⁵ / SSIM 0.8840 / PSNR 33.91 dB vs LI-MAR 1.4×10⁻⁴ / 0.8158 / 30.49, WNN-MAR 1.8×10⁻⁴ / 0.8029 / 29.61, and a from-scratch Deep-MAR 7.5×10⁻⁵ / 0.8605 / 32.68 — a 59% MSE reduction over LI-MAR, 68% over WNN-MAR, and a 23% relative MSE improvement of transfer learning over training from scratch, directly demonstrating the sim-to-real benefit. Inference is fast: the generator performs sinogram completion in **~58 ms on an NVIDIA Tesla P100** GPU, while the (unoptimised) CPU FBP step takes ~1.55 s per slice on an Intel Xeon 2.4 GHz, Linux, MATLAB R2017b. Limitations explicitly noted: (1) the method restricts itself to 2-D parallel-beam geometry; extension to fan/cone-beam is left to future work; (2) photon scatter is not modelled in the simulator (Monte-Carlo scatter is O(n⁶) and would prohibit the large simulated corpus) — the authors argue a simpler ray-based simulation suffices for sinogram completion training but note this would need revisiting for scatter-heavy clinical CT; (3) metal segmentation at inference relies on a 4,000-MHU threshold followed by erosion/dilation with radius-2/4 disk structuring elements — segmentation errors therefore propagate directly into the completion; (4) quantitative real-data evaluation uses virtually inserted metals because true before/after matched pairs are not physically obtainable in security scanners, which is a practical but not strictly ideal surrogate. \
[[arXiv](https://arxiv.org/abs/1904.04691)] IEEE Transactions on Computational Imaging, 2019. [[doi](https://doi.org/10.1109/TCI.2019.2937221)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/mughanibu/DeepMAR)

1. **DuDoNet: Dual Domain Network for CT Metal Artifact Reduction** <img src="https://img.shields.io/badge/Supervised-blue.svg" alt="Supervised"> \
*W.-A. Lin, H. Liao, C. Peng, X. Sun, J. Zhang, J. Luo, R. Chellappa, S. K. Zhou* \
**Summary** \
DuDoNet is the first end-to-end trainable dual-domain network for CT metal artifact reduction: it simultaneously enhances the metal-corrupted sinogram **and** the reconstructed image, and introduces a differentiable **Radon Inversion Layer (RIL)** that allows gradients to back-propagate from the image domain back into the sinogram domain during training — enabling the two branches to learn jointly rather than in two isolated stages. The pipeline has three components. (1) **SE-Net** (Sinogram Enhancement Net): takes the LI-repaired sinogram Y_LI and a metal-trace mask M_t as inputs and outputs a refined sinogram Y_out via a mask-pyramid-augmented U-Net, where the mask pyramid propagates metal-trace information across multiple scales so that small metal implants, whose trace vanishes in deep feature maps, are still respected — ablation shows the mask pyramid adds ~0.2 dB for small metals without hurting large-metal performance. (2) **RIL** (Radon Inversion Layer): converts the enhanced sinogram from fan-beam to parallel-beam, applies Ram-Lak filtering, and back-projects to the image X̂, so that the pipeline can compute a Radon-consistency loss L_RC directly in image space. (3) **IE-Net** (Image Enhancement Net): a residual U-Net that fuses X̂ and the LI-reconstructed image X_LI, outputting X_out = X_LI + G_i(X̂, X_LI). The full objective is L = L_Gs + L_RC + L_Gi with L_Gs an L1 on the enhanced sinogram, L_Gi an L1 on X_out, and L_RC the Radon-consistency term (the authors deliberately keep all three coefficients equal to 1 and report that tuning them is unnecessary). An interesting deliberate design choice is that DuDoNet refuses to take the raw metal-corrupted sinogram/image as input — it was experimentally found that feeding the raw inputs instead of LI-corrected ones leaves metal shadows and streaks uncorrected, so both branches start from the LI-repaired baseline. Data follows the DeepLesion synthesis protocol: 4,000 training CT images from 320 patients × 90 metal masks = **360,000** training combinations; 200 test CT × 10 metal masks = 2,000 test pairs; training metal sizes 16–4,967 pixels, test sizes 32–2,054 pixels. All CT images are resized to 416×416 and sinograms to 321×320 (320 projection views uniformly in 0–360°, source-to-rotation-center 39.7 cm), with polychromatic X-ray spectrum, 2×10⁷-photon Poisson noise and partial-volume effect simulated. Training is in PyTorch on **two NVIDIA 1080Ti** GPUs using Adam (β₁=0.5, β₂=0.999), batch size 8, initial learning rate 2×10⁻⁴ halved every 30 epochs, for 380 epochs total. On the DeepLesion synthetic test set (PSNR / SSIM by metal size, Large → Small): LI 20.20/0.8236, 22.35/0.8686, 26.76/0.9098, 28.50/0.9252, 29.53/0.9312 (avg 25.47/0.8917); NMAR avg 27.51/0.9001; cGAN-CT avg 28.07/0.8733; RDN-CT avg 31.74/0.9156; CNNMAR avg 29.52/0.9243; **DuDoNet** 29.02/0.8972, 31.12/0.9256, 34.32/0.9481, 36.72/0.9595, 36.36/0.9592 (avg **33.51 dB / 0.9379**). Ablation quantifies every contribution: SE-Net alone 26.46–26.71 avg PSNR, IE-Net alone 31.45, SE-Net + IE-Net (no RC loss) 32.95–32.96, and adding the Radon-consistency loss lifts this to 33.41–33.51 (+0.5 dB on average, +0.3 dB on every metal size), validating that RIL-enabled gradient flow between the two domains is essential. Qualitative comparisons on three metal-size regimes show that NMAR, cGAN-CT, RDN-CT and CNNMAR still leave shadowing or speckle noise in the middle / between implants, while DuDoNet cleanly removes both shadows and streaks and recovers fine organ details even for two-implant cases. The authors note several implicit limitations: (1) feeding the raw metal-corrupted sinogram/image directly does not work — DuDoNet needs a pre-processing LI step as input, making it a refinement rather than a fully end-to-end metal handler; (2) evaluation is synthetic-only in the main paper (clinical results are in supplementary material), with no quantitative clinical ground-truth metrics; (3) there is no explicit per-image inference time or parameter count reported in the main text beyond the dual-GPU training configuration. \
[[arXiv](https://arxiv.org/abs/1907.00273)] CVPR, 2019. [[doi](https://doi.org/10.1109/CVPR.2019.01076)] [![Open Access](https://img.shields.io/badge/Open%20Access-brightgreen.svg)](https://openaccess.thecvf.com/content_CVPR_2019/papers/Lin_DuDoNet_Dual_Domain_Network_for_CT_Metal_Artifact_Reduction_CVPR_2019_paper.pdf)

1. **ADN: Artifact Disentanglement Network for
Unsupervised Metal Artifact Reduction** \
*H. Liao et al.* <img src="https://img.shields.io/badge/Unsupervised-blue.svg" alt="Unsupervised"> \
**Summary** \
The authors propose to use a disentanglement network to separate the metal artifact and the non-metal artifact latent variables. Two encoders are used to encode the latent variables. The latent variables are used by the decoders to generate the metal artifact affected and metal artifact free images. The method is compared against linear interpolation, NMAR, U-Net, cGANMAR, and CycleGAN among other methods. U-Net had the best PSNR and cGANMAR had the best SSIM. ADN had best PSNR and SSIM among the compared unsupervised methods. \
[[arxiv](https://arxiv.org/pdf/1908.01104.pdf)]. IEEE TMI, 2019. [[doi](https://ieeexplore.ieee.org/document/8788607)] [![Code](https://img.shields.io/badge/Code-purple.svg)](https://github.com/liaohaofu/adn)
